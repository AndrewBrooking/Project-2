<section id="banner">

  <div class="content">

    <header>
      <% if(loggedIn) {%>
      <h1>Hi <%= userResult.uName %>, Welcome to PayMyProject</h1>
      <br>
      <% } else { %>
      <h1>Hi Guest, Welcome to PayMyProject</h1>
      <br>
      <% } %>

      <p>A fully responsive site to support your favorite content/project creators</p>

    </header>

    <% if(!loggedIn) {%>
    <p>Please create a user account to follow and donate to your favorite projects.</p>
    <% } %>
  </div>

</section>

<section>

  <header class="major">
    <h2>Current Open Projects</h2> <!-- Change to Following if following-->
  </header>

  <div class="posts">

    <% msg.forEach(function(item) { %>

    <article>
      <a class="image test"><img src=<%= item.img %> alt="" /></a>

      <h3><%= item.name %></h3>

      <p><%= item.desc %></p>

      <ul class="actions">

        <li><a href="/project/<%= item.id %>" class="button">More</a></li>

        <li>
          <form action="/following" method="post">
            <input type="submit" name="upvote" name2="name2" value="Follow" data-id="<%= item.id %>" />
          </form>
        </li>

      </ul>

    </article>

    <% }); %>

  </div>
</section>